def get_api_key(api_key: str) -> str:
    """
    Returns the provided API key.

    This function takes an API key as input and returns it. It is used to
    retrieve the API key for authentication purposes.

    :param api_key: The API key to return.
    :return: The provided API key.
    """
    return api_key
